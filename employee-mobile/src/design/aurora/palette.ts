/**
 * จานสีของทั้งแอป — จอที่ย้ายมาผิวออโรราแล้วต้องหยิบสีจากที่นี่ที่เดียว
 *
 * เดิมไฟล์นี้เป็นของหน้าหลักจอเดียวและห้ามจออื่น import ตอนนี้ไม่ใช่แล้ว:
 * ห้าจอหลัก (หน้าหลัก ลงเวลา อนุมัติ คำขอ เงินเดือน) เป็นต้นแบบของทั้งแอป
 * จอที่ยังใช้สีจาก useAppTheme() คือจอที่ยังไม่ได้ย้าย ไม่ใช่จอที่ตั้งใจให้ต่าง
 * รายละเอียดกติกาอยู่ใน AGENTS.md ที่รากโปรเจกต์
 *
 * โทน: **ฟ้า–ขาว** พื้นเป็นฟ้าอ่อนมีแสงฟ้าลอยอยู่ข้างหลัง การ์ดเป็นกระจกขาว
 * ฝ้าที่เห็นแสงทะลุ ตัวหนังสือเป็นสีเข้มปกติ — อ่านกลางแดดได้ ไม่แสบตากลางคืน
 *
 * สีสถานะ (เขียว/ส้ม/แดง) ไม่ถูกดึงเข้าโทนฟ้า เพราะมันคือ "ความหมาย"
 * ไม่ใช่การตกแต่ง — เตือนด้วยสีฟ้าไม่มีใครอ่านออกว่าเป็นคำเตือน
 */
export const AURORA = {
  /**
   * พื้นของทั้งแอป — **ฟ้าชัด ไม่ใช่ขาวอมฟ้า**
   *
   * นี่คือสีที่คนเห็นมากที่สุดในแอป ตอนที่ให้มันเกือบขาว (#e9eef7) ทั้งแอป
   * อ่านเป็น "แอปสีขาว" ไม่ใช่แอปโทนฟ้า และของสีขาวที่วางทับก็ไม่มีอะไรให้ลอย
   *
   * ค่านี้สว่างพอให้ตัวหนังสือสีเข้มอ่านได้สบาย และไม่แสบตาเวลาเปิดกลางคืน
   */
  base: '#e9eef7',
  /** ขาวล้วน — ใช้กับของที่ต้องลอยขึ้นจากพื้นฟ้าเท่านั้น ไม่ใช่พื้นทั้งจอ */
  baseDeep: '#ffffff',

  /**
   * ก้อนแสงสี่ก้อนที่ลอยอยู่หลังทุกอย่าง — ไล่จากน้ำเงินไปฟ้าอ่อน
   *
   * เข้มกว่าพื้นหนึ่งระดับ ไม่งั้นบนพื้นฟ้าที่ชัดขึ้นแล้วจะมองไม่เห็นก้อนแสงเลย
   */
  glowBlue: '#93b4f7',
  glowIndigo: '#b6cdf9',
  glowSky: '#7cd0f5',
  glowPale: '#aee0f7',

  /**
   * ผิวกระจก — ขาวโปร่ง เห็นแสงฟ้าทะลุขึ้นมาจาง ๆ
   * ทึบขึ้นกว่าโปร่งใสจัด เพราะตัวเลขที่ทับก้อนแสงพอดีจะอ่านยากทันที
   */
  glass: 'rgba(255, 255, 255, 0.96)',
  glassStrong: '#ffffff',
  /** แสงที่ตกบนผิวบนของกระจก ทำให้แผ่นดูนูนขึ้นมาจากพื้น */
  glassSheen: 'rgba(255, 255, 255, 0.85)',
  /**
   * เส้นขอบและเส้นคั่นในการ์ด ใช้ตัวเดียวกัน — ฟ้าจาง ไม่ใช่ขาว
   * (ขอบขาวบนการ์ดขาวจะหายไปเลย ไม่เหลือรูปทรงให้เห็น)
   */
  glassBorder: 'rgba(29, 78, 216, 0.13)',
  /** ไฮไลต์ขอบบนของการ์ด ทำให้กระจกดูมีความหนา */
  glassEdge: 'rgba(255, 255, 255, 0.95)',

  text: '#101828',
  textMuted: '#5a6478',
  textFaint: '#98a2b3',

  accent: '#1d4ed8',
  accentEnd: '#3b82f6',
  /** พื้นฟ้าจางของวงไอคอนที่อยู่ "บนการ์ดขาว" (บนพื้นจอต้องใช้สีทึบแทน) */
  accentSoft: 'rgba(29, 78, 216, 0.08)',
  /** ฟ้าสด ใช้กับค่าที่ต้องแยกจากตัวเลขหลักโดยไม่หลุดโทน (OT, วันลา) */
  sky: '#0284c7',

  /*
   * สีเตือนสองตัวนี้คือ **ข้อยกเว้นเดียว** ของโทนฟ้า–ขาว
   *
   * กติกา: ใช้ได้เฉพาะตอนที่ผู้ใช้ "ต้องลงมือทำอะไรบางอย่าง" — มาสาย ออกก่อนเวลา
   * ขาดงาน เวลาไม่ครบ โควตาใกล้หมด/หมดแล้ว ของค้างส่ง
   *
   * ห้ามใช้กับของที่แค่ "ดี" หรือ "เป็นปกติ" — มาปกติ ลงเวลาแล้ว OT ที่ได้เพิ่ม
   * ยอดเงิน วันลาที่ยังเหลือเยอะ ทั้งหมดนี้เป็นสีฟ้า เพราะถ้าทุกอย่างมีสีของตัวเอง
   * สีจะเลิกเป็นสัญญาณและกลายเป็นของตกแต่ง แล้วคำเตือนจริงจะจมหายไปในกองสี
   */
  amber: '#b45309',
  rose: '#be123c',
  /**
   * เขียวของสถานะ "อนุมัติแล้ว/สำเร็จ" — เขียวเดียวกับ `successText` ของจอเก่า
   * ที่ยังใช้ theme (เช่น `documents.tsx` `schedule.tsx`) ทั้งแอปให้ความหมายนี้
   * เป็นเขียวอยู่แล้ว จอออโรราที่เคยใช้ฟ้าแทนจึงเป็นจอนอกคอกไปเอง — ใบที่จบแล้ว
   * ควรได้สีเดียวกันไม่ว่าจะไปโผล่จอไหนของแอป
   */
  emerald: '#047857',
} as const;
