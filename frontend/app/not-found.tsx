import { ErrorScreen } from "@/components/common/error-screen";

/**
 * หน้าเมื่อเปิด URL ที่ไม่มีอยู่จริง
 *
 * เดิมไม่มีไฟล์นี้ Next.js จึงใช้หน้าเริ่มต้นภาษาอังกฤษที่ไม่มีทางกลับ
 * ซึ่งเกิดบ่อยเพราะเมนูถูกยุบรวมไปหลายรอบ ลิงก์เก่าที่บุ๊กมาร์กไว้จึงตายไปแล้ว
 */
export default function NotFound() {
  return (
    <ErrorScreen
      title="ไม่พบหน้าที่ต้องการ"
      description="ลิงก์นี้อาจถูกย้ายหรือถูกยุบรวมไปกับหน้าอื่นแล้ว ลองกลับหน้าแรกแล้วเข้าจากเมนูอีกครั้ง"
    />
  );
}
