"use client";

import { DayOffRosterPanel } from "./_components/day-off-roster-panel";

/**
 * เวรหยุดรายคน
 * -----------
 * คนละหน้ากับ ตั้งค่า › นโยบายการทำงาน › ปฏิทินวันหยุด โดยตั้งใจ
 *   หน้านั้น = ตั้งวันหยุดที่ทุกคนหยุดพร้อมกัน (วันหยุดประจำสัปดาห์ + นักขัตฤกษ์)
 *   หน้านี้  = ย้ายวันหยุดรายคนของทีมที่หมุนเวรกันเอง ซึ่งทำทุกสัปดาห์
 *
 * เหตุผลที่ต้องมีหน้านี้: ทีมจัดส่งกับการตลาดสลับกันมาทำงานวันอาทิตย์แล้วไปหยุด
 * วันธรรมดาแทน ถ้าไม่บันทึกไว้ ระบบจะนับวันที่เขาหยุดเป็น "ขาดงาน" แล้วหักเงิน
 * ฟอร์มสลับวันหยุดในหน้าตั้งค่ากรอกได้ทีละคนทีละคู่วัน ซึ่งไม่ไหวกับงานรายสัปดาห์
 * และมองไม่เห็นว่าวันไหนคนหยุดชนกันจนไม่เหลือคนส่งของ
 */
export default function DayOffRosterPage() {
  return <DayOffRosterPanel />;
}
