"use client";

import { LeaveQuotaPanel } from "./_components/leave-quota-panel";

/**
 * สิทธิ์วันลาพนักงาน
 * -----------------
 * คนละหน้ากับ ตั้งค่า › นโยบายการทำงาน › การลา โดยตั้งใจ
 *   หน้านั้น = ตั้งกติกาว่าประเภทลาไหนให้กี่วัน ใครได้บ้าง
 *   หน้านี้  = ดูผลจริงว่าพนักงานแต่ละคนเหลือเท่าไร และปรับเพิ่ม/ลดรายคน
 *
 * เหตุผลที่ต้องมีหน้านี้: ยอดวันลาถูกสร้างตอนพนักงานเปิดดูสิทธิ์ตัวเองเท่านั้น
 * คนที่ยังไม่เคยล็อกอินจึงไม่มียอดในระบบ และการแก้โควตาในหน้านโยบาย
 * ก็ไม่ย้อนไปแก้ยอดที่สร้างไว้แล้ว HR เลยไม่มีทางเห็นภาพรวมหรือแก้ไขได้เลย
 */
export default function LeaveQuotaPage() {
  return <LeaveQuotaPanel />;
}
