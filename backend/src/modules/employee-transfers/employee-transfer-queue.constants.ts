/**
 * งานตามตารางเวลาของใบโยกย้าย
 *
 * ใช้ repeatable job ของคิวกลางแทน cron ในโปรเซส ด้วยเหตุผลเดียวกับงานบำรุงรักษา:
 * ตอนรันหลาย instance cron ในโปรเซสจะยิงพร้อมกันทุกเครื่อง — ที่นี่แปลว่า
 * ใบเดียวถูกทำให้มีผลหลายรอบ และได้ประวัติการทำงานซ้ำเป็นจำนวนเท่ากับจำนวนเครื่อง
 */
export const EMPLOYEE_TRANSFER_QUEUE = 'employee-transfer';

export const EMPLOYEE_TRANSFER_JOB_APPLY_DUE = 'apply-due-transfers';

/**
 * ตีหนึ่งครึ่งทุกวัน (เวลาไทย)
 *
 * ต้องผ่านเที่ยงคืนไปแล้วเพื่อให้ "วันนี้" เป็นวันใหม่จริง และเสร็จก่อนงานสำรอง
 * ข้อมูลตอนตีสอง เพื่อให้สำเนาของคืนนั้นมีสังกัดใหม่อยู่แล้ว
 */
export const EMPLOYEE_TRANSFER_APPLY_PATTERN = '30 1 * * *';
