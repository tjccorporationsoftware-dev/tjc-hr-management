# ไม่ส่งของพวกนี้เข้า build context — ทำให้ build ช้าและอาจหลุดค่าลับเข้าอิมเมจ
node_modules
dist
coverage
.env
.env.*
!.env.example
*.log
tsconfig.tsbuildinfo

# ไฟล์ที่ผู้ใช้อัปโหลด/ระบบสร้าง — ต้อง mount เป็น volume ไม่ใช่อบเข้าอิมเมจ
uploads
storage
backups
cleanup-logs

# ไฟล์ที่สร้างใหม่ได้ตอน build
src/generated
