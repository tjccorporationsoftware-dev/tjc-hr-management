-- ขอบเขตสาขาของรอบคำนวณเงินเดือน
--
-- ก่อนหน้านี้กดคำนวณทีได้พนักงาน "ทั้งบริษัท" เสมอ บริษัทที่มีหลายสาขา
-- จึงแยกรอบจ่ายรายสาขาไม่ได้เลย ทั้งที่แต่ละสาขามีงวดและบัญชีธนาคารของตัวเอง
--
-- เก็บเป็น array ของ branch id ไม่ใช่ relation เพราะเป็นภาพนิ่งของตอนสร้างรอบ
-- ถ้าภายหลังมีการลบสาขา รอบเก่าต้องยังบอกได้ว่าตอนนั้นคำนวณให้สาขาไหนบ้าง
--
-- ค่าเริ่มต้นเป็น array ว่าง = "ทั้งบริษัท" ซึ่งเป็นพฤติกรรมเดิมเป๊ะ
-- รอบที่สร้างไว้ก่อนหน้านี้จึงไม่เปลี่ยนผลลัพธ์

ALTER TABLE "payroll_runs"
  ADD COLUMN IF NOT EXISTS "branchIds" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
