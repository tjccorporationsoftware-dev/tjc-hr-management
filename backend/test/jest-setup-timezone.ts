/**
 * บังคับให้เทสรันบนเซิร์ฟเวอร์ UTC
 *
 * เครื่องของทีมตั้งเป็นเวลาไทย แต่ production รันบนคอนเทนเนอร์ที่เป็น UTC
 * บั๊กวันที่คลาดไป 1 วันจึงไม่มีทางโผล่ตอนพัฒนา เห็นอีกทีตอนขึ้นระบบจริง
 *
 * ตั้งไว้ตรงนี้เพื่อให้ทุกเทสเจอสภาพเดียวกับ production
 * โค้ดที่ตัดวันต้องใช้ common/utils/thai-date.util ซึ่งระบุเขตเวลาเอง
 * ไม่ใช่ `date.getFullYear()` ที่อ่านตามเวลาเซิร์ฟเวอร์
 */
process.env.TZ = 'UTC';
