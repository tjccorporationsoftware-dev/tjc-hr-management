# ขึ้นระบบบนเซิร์ฟเวอร์จริง

คู่มือนี้ใช้กับการติดตั้งด้วย **Docker บน Linux**
ถ้าติดตั้งตรงบน **Windows Server** ให้ดู [DEPLOYMENT-WINDOWS.md](DEPLOYMENT-WINDOWS.md) แทน

ทุกคำสั่งรันจากโฟลเดอร์บนสุดของโปรเจกต์ (ที่มีไฟล์ `docker-compose.prod.yml`)

> ระบบนี้ไม่มีการส่งอีเมล พนักงานที่ลืมรหัสผ่านต้องให้ HR กดรีเซ็ตให้จากหน้า
> ผู้ใช้งานระบบ ไม่มีปุ่ม "ลืมรหัสผ่าน" ให้กดเอง — ต้องแจ้งลูกค้าก่อนเปิดใช้

---

## สิ่งที่ต้องมีบนเซิร์ฟเวอร์

| อย่าง | เหตุผล |
|---|---|
| Docker + Docker Compose v2 | รันทั้งระบบ |
| พื้นที่ว่าง ≥ 20 GB | อิมเมจ backend มี Chromium สำหรับออก PDF |
| RAM ≥ 4 GB | Postgres + Node สองตัว (Redis ไม่จำเป็นแล้ว ดู QUEUE_DRIVER) |
| โดเมนจริง + ใบรับรอง HTTPS | ระบบบังคับ `https` และ cookie แบบ secure |
| reverse proxy (nginx / Caddy) | คอนเทนเนอร์เปิดเฉพาะ `127.0.0.1` ไม่รับตรงจากอินเทอร์เน็ต |

**เขตเวลาต้องเป็น `Asia/Bangkok`** ทั้งเครื่องโฮสต์และในคอนเทนเนอร์
การคำนวณเวลาเข้างานและรอบเงินเดือนอิงเขตเวลานี้ ตั้งผิดแล้วยอดเพี้ยนทั้งงวด

---

## ติดตั้งครั้งแรก

### 1. เตรียมค่าตั้งระบบ

```bash
cp backend/.env.example backend/.env
cd backend && npm run gen:secrets     # สุ่มค่าลับ คัดลอกไปวางใน .env
```

แล้วเติมค่าที่เหลือใน `backend/.env` ให้ครบ — ทุกบรรทัดที่เป็น `<<< ... >>>`

สร้าง `.env` อีกไฟล์ที่โฟลเดอร์บนสุด สำหรับ `docker-compose.prod.yml` อ่าน:

```bash
cat > .env <<'EOF'
POSTGRES_USER=hr_admin
POSTGRES_PASSWORD=<<<ค่าเดียวกับที่อยู่ใน DATABASE_URL>>>
POSTGRES_DB=hr_workforce
NEXT_PUBLIC_API_BASE_URL=https://<<<โดเมน>>>/api
EOF
```

> `NEXT_PUBLIC_API_BASE_URL` ถูก**ฝังลงบันเดิลตอน build** เปลี่ยนทีหลังตอนรันไม่ได้
> ถ้าเปลี่ยนโดเมนต้อง build frontend ใหม่

### 2. ตรวจค่าตั้งระบบก่อนสร้างอะไร

```bash
cd backend && npm run check:prod
```

**ต้องไม่มี `[FAIL]` เหลือเลย** ก่อนไปขั้นถัดไป
`[WARN]` อ่านให้เข้าใจว่าแลกอะไร แล้วค่อยตัดสินใจปล่อยผ่าน

### 3. สร้างและเปิดระบบ

```bash
docker compose -f docker-compose.prod.yml up -d --build
```

### 4. สร้างตารางในฐานข้อมูล

**ต้องสั่งเอง อิมเมจไม่รันให้** — เพราะถ้ารันหลาย instance พร้อมกันจะแย่งกันแก้สคีมา

```bash
docker compose -f docker-compose.prod.yml run --rm backend npm run db:migrate:deploy
```

### 5. ใส่ข้อมูล — เลือกทางใดทางหนึ่ง

**ทาง ก. ย้ายข้อมูลจากเครื่องที่ทำไว้แล้ว** (แนะนำ — ทะเบียนพนักงาน โครงสร้างองค์กร
เวลาเข้างาน และงวดเงินเดือนที่ตรวจแล้วอยู่ในนั้นครบ)

```bash
# บนเครื่องเดิม
docker exec hr_postgres pg_dump -U hr_admin -d hr_workforce -Fc > hr.dump

# คัดลอกไฟล์ขึ้นเซิร์ฟเวอร์ แล้วบนเซิร์ฟเวอร์
docker cp hr.dump hr_postgres_prod:/tmp/hr.dump
docker exec hr_postgres_prod pg_restore -U hr_admin -d hr_workforce \
  --clean --if-exists /tmp/hr.dump
docker exec hr_postgres_prod rm /tmp/hr.dump
```

ย้ายไฟล์ที่ผู้ใช้อัปโหลดตามไปด้วย ไม่งั้นรูปพนักงานกับโลโก้บริษัทหาย:

```bash
docker cp <เครื่องเดิม>:/app/uploads ./uploads
docker cp ./uploads hr_backend:/app/uploads
```

**ทาง ข. เริ่มจากฐานข้อมูลเปล่า**

```bash
docker compose -f docker-compose.prod.yml run --rm backend npm run db:seed
```

ได้แค่โครงสิทธิ์กับบัญชี superadmin ต้องกรอกข้อมูลพนักงานเองทั้งหมด

### 6. ตรวจว่าขึ้นจริง

```bash
curl -s http://127.0.0.1:4000/api/health   # ต้องตอบ ok และเช็คฐานข้อมูลจริง
curl -sI http://127.0.0.1:3000 | head -1   # ต้องเป็น 200
docker compose -f docker-compose.prod.yml ps
```

`/api/health` ตรวจการต่อฐานข้อมูลจริง ไม่ใช่ตอบ 200 เปล่า ๆ เชื่อผลได้

### 7. ตั้งงานตามเวลา

งานสำรองข้อมูลใช้ `scripts/backup-host.sh` ซึ่งต้อง**รันจากเครื่องโฮสต์**
(`backend/scripts/backup.ts` เรียก `docker exec` จึงรันจากในคอนเทนเนอร์ไม่ได้
และหาไฟล์อัปโหลดไม่เจอเพราะ compose เก็บไว้เป็น named volume ไม่ใช่โฟลเดอร์บนโฮสต์)

ส่วนงานล้างไฟล์รันในคอนเทนเนอร์ได้ตามปกติ

```cron
# สำรองฐานข้อมูล + ไฟล์อัปโหลด ทุกคืนตีสอง
0 2 * * *  cd /srv/hr && ./scripts/backup-host.sh >> /var/log/hr-backup.log 2>&1

# ล้างไฟล์ชั่วคราวและไฟล์ส่งออกที่หมดอายุ ตีสามครึ่ง
30 3 * * * cd /srv/hr && docker compose -f docker-compose.prod.yml exec -T backend npm run cleanup:temp
40 3 * * * cd /srv/hr && docker compose -f docker-compose.prod.yml exec -T backend npm run cleanup:exports

# ล้างบันทึกตรวจสอบที่เกินอายุ ทุกวันอาทิตย์ตีสี่
0 4 * * 0  cd /srv/hr && docker compose -f docker-compose.prod.yml exec -T backend npm run cleanup:audit
```

ไฟล์สำรองไปอยู่ที่ `backups/backup-<วันเวลา>/` บนโฮสต์ **คัดลอกออกนอกเครื่องด้วย**
เก็บไว้บนเครื่องเดียวกับระบบไม่ได้ช่วยอะไรตอนเครื่องพัง

**ทดสอบกู้คืนอย่างน้อยหนึ่งครั้ง** ก่อนบอกว่าพร้อมใช้
การสำรองข้อมูลที่ยังไม่เคยกู้กลับมาได้ ไม่นับว่ามีการสำรองข้อมูล

---

## อัปเดตครั้งถัดไป

```bash
git pull
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml run --rm backend npm run db:migrate:deploy
docker compose -f docker-compose.prod.yml up -d
```

ลำดับสำคัญ — **migrate ก่อน `up -d` เสมอ** ไม่งั้นโค้ดใหม่จะเจอตารางเก่า

ย้อนกลับ: `git checkout <commit เดิม>` แล้ว build ใหม่
migration ย้อนอัตโนมัติไม่ได้ ถ้าต้องย้อนสคีมาให้กู้จากไฟล์สำรองของคืนก่อน

---

## ที่ต้องกรอกก่อนใช้งานจริงแต่ละเรื่อง

ระบบขึ้นได้โดยไม่มีของพวกนี้ แต่ฟีเจอร์ที่เกี่ยวข้องจะใช้ไม่ได้

| ต้องกรอก | ที่ไหน | ไม่มีแล้วทำอะไรไม่ได้ |
|---|---|---|
| เลขประจำตัวผู้เสียภาษี + ที่อยู่บริษัท | ตั้งค่า › ข้อมูลบริษัท | หัวแบบ ภ.ง.ด.1 ไม่สมบูรณ์ |
| เลขที่บัญชีนายจ้าง (สปส.) | ตั้งค่า › ข้อมูลบริษัท | ยื่น สปส.1-10 ไม่ได้ |
| รหัสบริษัทที่ธนาคารออกให้ + บัญชีตัดจ่าย | ตั้งค่า › เงินเดือน | สร้างไฟล์โอนเงิน KTB ไม่ได้ |
| ชื่อผู้ลงนาม | ตั้งค่าระบบ › แม่แบบเอกสาร | ออกหนังสือรับรองเงินเดือนไม่ได้ |
| ยอดรายได้สะสมยกมา | พนักงาน › ภาษี | บริษัทที่เริ่มใช้กลางปีจะหักภาษีต่ำกว่าจริง |

---

## เชื่อมเครื่องสแกนลายนิ้วมือ

ข้ามได้ถ้ายังไม่ใช้เครื่องสแกน ระบบขึ้นได้ตามปกติโดยไม่ต้องทำส่วนนี้

ระบบรับข้อมูลด้วยโปรโตคอล **ADMS/iclock** (เครื่องยิงเข้ามาเอง ไม่ต้องมีคนกด
ดึงข้อมูล) เครื่อง ZKTeco และรุ่นที่ใช้โปรโตคอลเดียวกันใช้ได้ทั้งหมด

> หัวข้อนี้เขียนไว้สำหรับการรันด้วย docker compose
> **ไม่ใช้ docker → ดู DEPLOYMENT-WINDOWS.md หัวข้อ 7**
> **ต่อกับเครื่องที่กำลังพัฒนา → ไม่ต้องมีตัวคั่นหน้า** ชี้ตรงที่พอร์ต 4000 ได้เลย
> ทั้งสองกรณีอยู่ใน `infra/attendance-device-setup.md`

### 1. ตั้งโทเคนให้ตรงกันสองไฟล์

`npm run gen:secrets` สุ่ม `ATTENDANCE_DEVICE_PUSH_TOKEN` มาให้แล้ว
เอาค่านั้นวางทั้งใน `backend/.env` **และ** `.env` ที่โฟลเดอร์บนสุด

ไม่ตรงกัน = เครื่องสแกนได้ 401 ข้อมูลลงเวลาไม่เข้า และหน้าเว็บไม่ขึ้นอะไรเลย

### 2. เปิดตัวคั่นหน้า

```bash
docker compose -f docker-compose.prod.yml up -d device-proxy
```

เครื่องสแกนตั้งค่าได้แค่ที่อยู่กับพอร์ต แนบโทเคนเองไม่ได้ service นี้จึงคอยเติมให้
(ดู `infra/nginx/README.md`) **อย่าเปิดพอร์ต 8080 ออกอินเทอร์เน็ต** —
เครื่องพูด HTTP ล้วน ต่อ TLS ไม่ได้ ให้อยู่วงเดียวกันหรือลากผ่าน VPN

### 3. เพิ่มเครื่องในระบบ

ตั้งค่า › การลงเวลา › แท็บเครื่องสแกน → เพิ่มเครื่อง

**Serial Number ต้องตรงกับที่พิมพ์ติดตัวเครื่องเป๊ะ ๆ** ระบบหาเครื่องด้วยเลขนี้
ไม่ตรง = ข้อมูลถูกปฏิเสธ (ไม่หาย เครื่องเก็บไว้ยิงซ้ำจนกว่าจะแก้ SN ถูก)

### 4. ผูกพนักงานกับรหัสในเครื่อง

เครื่องส่งมาแค่รหัสผู้ใช้ (enroll no.) ต้องผูกว่ารหัสไหนคือพนักงานคนไหน
ที่ปุ่มผูกพนักงานในหน้าเดียวกัน **ที่ยังไม่ผูก = สแกนแล้วตกเป็นรายการไม่มีเจ้าของ
ไม่กลายเป็นการลงเวลา** (ดูได้ในประวัติการสแกนของเครื่อง)

ตัวลายนิ้วมือยังต้องไปเก็บที่ตัวเครื่องเอง ระบบไม่ push ทะเบียนผู้ใช้ลงเครื่องให้

### 5. ตั้งค่าที่ตัวเครื่อง

เมนู Comm / Cloud Server / ADMS ของเครื่อง

| ช่อง | ค่า |
|---|---|
| Server Address | IP ของเซิร์ฟเวอร์ที่รัน device-proxy |
| Server Port | 8080 |
| Enable Domain Name | ปิด (ถ้าเครื่องมีช่องนี้) |
| Time Zone | GMT+7 |

ระบบตีความเวลาที่เครื่องส่งมาเป็นเวลาไทยเสมอ **เครื่องตั้งเวลาผิด = ลงเวลาผิด
ตามไปทั้งงวด** ไม่มีตัวไหนจับให้

### ตรวจว่าใช้ได้จริง

```bash
curl -i "http://<ที่อยู่เซิร์ฟเวอร์>:8080/iclock/cdata?SN=TEST123"
```

ได้ `GET OPTION FROM: TEST123` พร้อมค่า config = ทางเดินครบแล้ว
ได้ `401` = โทเคนสองไฟล์ไม่ตรงกัน ไม่มีคำตอบ = firewall หรือพอร์ตไม่ได้ map

จากนั้นให้คนไปแตะนิ้วที่เครื่องจริง แล้วดูที่ ตั้งค่า › การลงเวลา › เครื่องสแกน →
ประวัติการสแกน ควรขึ้นภายในไม่กี่วินาที

### ที่ยังไม่ทำ

- ไม่ push ทะเบียนผู้ใช้/ลายนิ้วมือลงเครื่อง ต้อง enroll ที่ตัวเครื่องเอง
- ไม่เก็บรูปถ่ายตอนสแกน (รับแล้วทิ้ง)
- แตะซ้ำภายใน 3 นาทีถือเป็นการสแกนซ้ำ ตัดทิ้งอัตโนมัติ

---

## เรื่องความปลอดภัยที่ห้ามลืม

- **อย่าเปิดพอร์ต 5432 / 6379 ออกอินเทอร์เน็ต** — compose ผูกไว้ที่ `127.0.0.1` แล้ว
  อย่าไปแก้เป็น `0.0.0.0`
- **`TRUST_PROXY=true`** เมื่ออยู่หลัง reverse proxy ไม่งั้นระบบเห็น IP ของ proxy
  แทน IP ผู้ใช้จริง การล็อกบัญชีหลังใส่รหัสผิดหลายครั้งจะไร้ผลทันที
- **`TWO_FACTOR_DEV_SHOW_CODE` ต้องเป็น false** — true คือโชว์รหัสยืนยันออกมาตรง ๆ
- **เปลี่ยนรหัส superadmin หลังติดตั้งเสร็จ** และเก็บไว้นอกเครื่อง
- ไฟล์ `backend/.env`, โฟลเดอร์ `credentials/` และ `docs/*.xlsx`
  ถูก git ignore ไว้แล้ว — มีเลขบัตรประชาชนและเงินเดือนรายคนอยู่ในนั้น
  **อย่าใช้ `git add -f` กับไฟล์พวกนี้** ลบออกจากประวัติย้อนหลังทำได้ยากมาก

---

## ที่ยังไม่พร้อมและต้องรู้

- **แอปมือถือยังใช้จริงไม่ได้** ตอนนี้รันผ่าน Expo Go เพื่อทดสอบเท่านั้น
  ผู้ใช้จริงต้องรอ build ขึ้น App Store / Play Store ก่อน
  (`MOBILE_IOS_STORE_URL` / `MOBILE_ANDROID_STORE_URL` ปล่อยว่างไว้จนถึงตอนนั้น
  หน้าเว็บจะไม่โชว์ปุ่มดาวน์โหลด)
- **ยืนยันตัวตนสองชั้นปิดอยู่** (`TWO_FACTOR_ENABLED=false`) รอเฟส TOTP
- **ไม่มีระบบส่งอีเมล** ไม่มี SMTP ในระบบเลย การแจ้งเตือนทั้งหมดอยู่ในแอปและ push
  ของมือถือเท่านั้น
